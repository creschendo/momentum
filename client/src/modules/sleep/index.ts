// Module metadata for the Sleep module — consumed by the module registry to register the route and sidebar entry.
const sleep = {
  key: 'sleep',
  title: 'Sleep',
  description: 'Log sleep sessions and monitor recovery trends.'
};

export default sleep;
