// Module metadata for the Pomodoro module — consumed by the module registry to register the route and sidebar entry.
const pomodoro = {
  key: 'pomodoro',
  title: 'Pomodoro',
  description: 'Focus sessions with built-in breaks.'
} as const;

export default pomodoro;
